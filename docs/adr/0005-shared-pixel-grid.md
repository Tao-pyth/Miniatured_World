# ADR 0005: 前景の論理ピクセル面

状態: 採用（v0.9.3）

## 判断

個々の高解像素材を別倍率で描く方式では、作画と拡縮の違いが露出する。共通64色・論理ピクセル寸法の素材を使い、すべての前景を640×427へ描いて最近傍2倍で合成する。ウィンドウへの全景倍率は共通のまま維持する。

## 影響

背景、入力取得、World、永続化の境界は変えない。描画フレームごとに小さな透過面を使用する。素材・ページめくり・影・完成品にも同じピクセル単位が適用される。画像処理用Pillowは開発時のみで、アプリ実行時へ追加しない。
