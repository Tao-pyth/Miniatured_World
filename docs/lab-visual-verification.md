# v0.9.1 ビジュアル検証記録

2026-09-05、Windows 11 / Python 3.13.7 / PySide6 6.9.3。

| 条件 | 証拠 | 結果 |
| --- | --- | --- |
| 5ポーズの全身・帽子・成功の両端 | 個別PNGとQt 5状態レンダリングを目視 | 欠け、隣接絵、説明文字なし |
| 白い顔・服と透明背景 | 境界に接続した背景だけを透過、画像拡大確認 | 顔や服の抜けを修正 |
| 釜4系列各8枚 | 32枚の存在・ピクセル差分・共通台座基準を検査 | 8fpsループ、全コマ異なる |
| 画面配置 | 640×427、1280×720、900×620のQtレンダリング | 縦横比・足元・全景を維持 |
| 入力更新との分離 | 実Qtタイマー180ms待機中の画像差分とRuntime snapshot同一性 | 画像のみ変化 |
| 非表示・一時停止・他タブ・終了 | タイマー状態と経過時間を検査 | 停止と復帰を確認 |
| 成功固定の防止 | 保存済み発見、新規差分、3.2秒経過、再通知 | 新規差分のみ短時間反応 |
| 素材・光・煙 | Qt静止画と64コマGIF | 小さな粒子、釜へ流入、人物を覆う円なし |
| 回帰・プライバシー | `.venv\Scripts\python.exe -m pytest` | 52 passed |
| wheel | 別ディレクトリへ展開してimport、バージョンとQt描画を検査 | 0.9.1、39素材一致 |
| exe | PyInstaller archive内の素材をソースと比較 | 39素材一致 |
| Windows実活動GUI | `--ephemeral --activity-provider windows-global --duration-seconds 30 --stability-log ...` | completed、30フレーム、exit 0 |
| exe CLI | `--no-ui --ephemeral --activity-provider demo --frames 5` | exit 0 |

ローカル検証出力は `logs/lab-visual-quality/`、`logs/v0.9.1-exe-gui-smoke.jsonl`。GUI検証は30.344秒、CPU時間0.609秒、最終RSS約72.9MiB。短時間の単一環境測定であり、長期安定性や全環境の性能保証ではない。

実画面キャプチャは行っていない。プレビューはデモスナップショットをQtで描画したもので、他のアプリや実入力を含まない。ネイティブGUIのログも抽象化された活動のみを記録する。

元シートの差分に由来する形状の揺れ、追加の歩行コマ、高DPI/複数モニター実機検証、新版の8時間検証は残件として維持する。リリース公開とダウンロード照合の結果はOODA Act記録へ残す。
