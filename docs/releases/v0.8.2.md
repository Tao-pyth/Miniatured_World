# v0.8.2 Release Notes

`v0.8.2` は、MVP RC前に表示・非干渉品質を狭く固める検証リリースです。

## 追加

- Windowsでクリック透過有効時に、拡張ウィンドウスタイル `WS_EX_LAYERED` と `WS_EX_TRANSPARENT` を補助的に適用するようにした。
- 表示モード、クリック透過、不透明度の自動テストを拡充した。
- 表示/非表示、トレイ復帰、クリック透過の手動検証手順を追加した。

## 検証

- `python -m pytest`: 42 passed
- `git diff --check`: pass
- GUI短時間smoke: completed / error 0
- Artifact: `dist\MiniaturedWorld.exe`
- Artifact SHA256: `A89DB6F1823561CF34090D9971CD3E40508E6B48FD963894CB43836179E1E0D9`
- Artifact size: 48933238 bytes

## 既知制限

- MVP RC合格宣言ではありません。
- 全アプリ・全環境で保証された完全クリック透過ではありません。
- 高DPI、マルチモニター、アンチチート搭載ゲームとの互換性は完全保証しません。
- インストーラー、コード署名、自動更新は未対応です。

## 日本語説明（初心者向け）

このリリースでは、デスクトップ表示中に通常作業を邪魔しにくくするため、Windows向けのクリック透過補助を追加しました。アプリはWindow handleとウィンドウスタイルだけを扱い、入力文字列、キー列、マウス座標、Window Title、画面内容、クリップボード内容は保存しません。
