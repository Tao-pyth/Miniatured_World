# Miniatured World Roadmap

このロードマップは、企画・要求仕様書を実装フェーズへ分解するための作業用正本です。詳細なスコープ、受け入れ条件、リリース契約は OODA ワークフローで確定します。

## バージョン方針

- ユーザー向けフェーズ表記は `V0.0`, `V0.1`, `V0.2` の形式を使う。
- OODA記録、Gitタグ、Releaseは `v0.0.0` のようなセマンティックバージョンを使う。
- 現在のリリース済みベースラインは `V0.9 / v0.9.9`。
- 現在の次候補は `V1.0 / v1.0.0` のMVP条件の検証。互換性・常駐品質の合否を別途判断する。

## V0.0: プロジェクト骨子

目的: 規律ある開発を始めるための最小構成を作る。

スコープ:

- リポジトリ構成
- README
- Roadmap
- エージェント運用ルール
- 正本仕様書への参照
- 初期OODAベースライン

完了済み。

## V0.1: 技術PoC

目的: 安全な活動信号を扱い、Windows常駐アプリとしてユーザー作業を邪魔しない実現性を確認する。

現在の状態: コアの一部は実装済み。本番用Windows入力取得と常駐品質は未確定。

候補スコープ:

- Windows 11 デスクトップアプリの骨格
- PySide6 アプリケーション起動
- タスクトレイ常駐
- キーボード活動カテゴリ化の検証
- マウス活動集約の検証
- プライバシーフィルタの検証
- 活動フレームモデル
- 最小ワールド表示
- seed付きシミュレーションループ
- Raw Input を永続保存しない証拠

主要判断:

- Windows入力APIの選定
- Low-Level Hook採用可否
- 活動集約間隔
- Direct Interaction と Ambient Input の分離
- 最小プライバシーテスト戦略

## V0.2: シミュレーションプロトタイプ

目的: 抽象化されたPC活動が、小さな創発的ワールドを生成できることを確認する。

現在の状態: 決定的シミュレーションコア、コンテンツカタログ、実行状態、活動プロバイダー境界、発見管理、プライバシー安全なスナップショットを実装済み。

候補スコープ:

- 素材定義
- 落下物または粒子表現
- グリッドまたはセル型シミュレーション
- 簡易植物ライフサイクル
- 1体の生物の有限状態行動
- 傾向と特性による修飾
- イベントルール
- 希少現象フック
- 決定的乱数プロバイダー

主要判断:

- シミュレーション更新頻度
- 粒子数と生物数の上限
- コンテンツ定義形式
- レシピ/ルール評価モデル

## V0.3: プロダクトプロトタイプ

目的: シミュレーションを、使えるデスクトッププロダクトの形へ近づける。

現在の状態: 実行コマンドと PySide6 のウィンドウ表示・設定・発見画面を実装済み。デスクトップ表示のクリック透過、実トレイ常駐品質、視覚品質検証は継続課題。

候補スコープ:

- ウィンドウ表示
- デスクトップ表示の実現性
- 設定画面
- 発見画面
- 一時停止と再開
- 表示/非表示
- 基本通知
- サウンド切替
- 設定と発見情報の原子的永続化
- サブシステム単位のエラー分離
- 性能品質コントロール

主要判断:

- クリック透過デスクトップウィンドウ方式
- 高DPI挙動
- ユーザーデータ保存先
- 発見スキーマ
- クラッシュと復旧方針

## V0.4: PySide6画面基盤

目的: MVPに必要なアプリ画面の最小面を揃える。

状態: リリース済み。

完了内容:

- ウィンドウ表示
- 設定画面
- 発見画面
- 実行コマンド
- プライバシー安全なスナップショット
- Qt offscreen スモークテスト

未完了:

- 本番用Windows入力取得
- デスクトップ表示
- インストーラー
- 長時間安定性検証

## V0.5: 永続化とトレイ基盤

目的: ローカルアプリとして継続利用できる最低限の保存・設定・常駐基盤を作る。

状態: リリース済み。

スコープ:

- 既定データ保存先
- 設定の読み込み/保存
- 設定画面からの実行時設定更新
- 発見情報の保存可否設定
- Qtトレイメニュー基盤
- `--data-root` と `--ephemeral`
- ユーザー向けアウトプットの日本語統一
- 永続化とQtスモークのテスト

非スコープ:

- 本番用Windowsグローバル入力取得
- デスクトップ表示のクリック透過
- 通知の本番実装
- 音声再生
- インストーラー

## V0.6: Windows活動取得PoC

目的: プライバシー不変条件を守りながら、実PC活動を抽象信号へ変換できることを検証する。

状態: リリース済み。最初のPoCでは、Raw Input本実装ではなく、Windowsの最終入力時刻からアイドル時間だけを取得する安全な活動取得元を追加した。

スコープ:

- Windows入力取得方式の比較
- Raw Inputを扱わないWindowsアイドル取得元
- 活動取得状態モデル
- 取得元選択CLI
- Runtime Snapshotへの活動取得状態追加
- World画面の活動取得状態表示
- 取得不能時の安全なフォールバック
- Privacy Filter の境界強化
- Raw Input 非保存テスト
- 活動集約器との接続
- 失敗時の安全停止

非スコープ:

- Raw Input本実装
- Low-Level Hook採用
- キーボード/マウスカテゴリの実機グローバル取得
- 長時間常駐時の性能測定

## V0.7: デスクトップ表示PoC

目的: 通常作業を邪魔しないデスクトップ表示方式を検証する。

状態: リリース済み。最初のPoCでは、既存の表示設定をPySide6ウィンドウ属性へ反映し、アプリ再起動なしで表示挙動が変わる足場を作った。

スコープ:

- 透過/クリック透過ウィンドウ
- 最前面制御
- 表示モード設定のQt属性反映
- 不透明度設定のQt window opacity反映
- Settings画面からの反映確認
- offscreen Qt スモークテスト
- ウィンドウ表示との切替

非スコープ:

- Windows固有APIによるOSレベルの完全クリック透過
- 高DPI対応
- マルチモニターの最低限確認
- 表示品質の実機視覚確認

## V0.8: MVP RC準備版

目的: MVP Release Candidate本体へ進むために、実活動取得、配布、検証手順、既知制限、公開前方針を揃える。

状態: リリース済み。MVP RC合格宣言ではなく、RC判定材料を揃える準備版として扱う。

スコープ:

- プライバシー検証
- Windows実活動Provider
- PyInstaller exeビルド手順
- 8時間安定性テスト手順
- Windows互換性確認手順
- 高DPI/マルチモニター検証手順
- ユーザー向けドキュメント
- MIT License
- GitHub Issueのみのサポート方針
- 外部通信なしの明記
- リリースパッケージ候補

非スコープ:

- MVP RC合格宣言
- OSレベルで保証された完全クリック透過
- msi等のインストーラー
- コード署名
- 自動更新
- 外部通信、テレメトリ、クラウド同期
- アンチチート搭載ゲームとの互換性保証

## V0.8.1: GUIあり安定性検証

目的: GUIイベントループ、Qt描画、ウィンドウ更新を含めた8時間安定性を確認する。

状態: リリース済み。GUIあり安定性ログ、自動終了、PySide6 6.9系単体exeの短時間smoke、実機8時間検証を完了した。

完了内容:

- GUI起動時の `--stability-log`
- GUI起動時の `--duration-seconds` 自動終了
- GUI起動時の `--tick-interval-ms` 反映
- GUIあり8時間安定性検証手順
- PySide6 6.9系単体exeのGUI短時間smoke
- PySide6 6.9系単体exeのGUIあり8時間実機検証

非スコープ:

- GUIデザイン刷新
- 高DPI/マルチモニター完全対応
- コード署名
- インストーラー
- 自動更新

## V0.8.2: 表示・非干渉品質

目的: MVP RC前に、Windows 11上での表示モード、最前面、透明度、クリック透過、トレイ復帰の基本品質を狭く固める。

状態: リリース済み。Windowsクリック透過補助、表示設定テスト、表示・非干渉品質の検証手順を追加した。

完了内容:

- Windows拡張ウィンドウスタイルによるクリック透過補助
- 表示モード、クリック透過、不透明度の自動テスト拡充
- 表示/非表示とトレイ復帰の手動検証手順
- PyInstaller単体exeのGUI短時間smoke

非スコープ:

- GUIデザイン刷新
- 全環境で保証された完全クリック透過
- 高DPI/マルチモニター完全対応
- インストーラー
- コード署名
- 自動更新

## V0.9: 小さなラボラトリー表現リメイク

目的: 箱庭世界の全面描写から、PC活動で素材が届く小さなラボ鑑賞体験へユーザー向け表現を切り替える。

状態: リリース済み。

スコープ:

- プロダクト表現を「小さなラボラトリー」へ更新
- 基準背景画像の同梱
- 背景、錬金釜、錬金術師キャラクター、エフェクト、UIのレイヤー分離
- キャラクターの最小状態 `idle` / `work` / `success` / `failure` / `rest`
- 錬金釜の最小状態 `idle` / `receive` / `react` / `success` / `failure`
- 素材流入、反応光、煙の最小エフェクト
- 既存のActivity、Privacy Filter、Aggregator、Material、Event、Discovery基盤の維持

非スコープ:

- 内部Worldモデル名の全面改名
- 複数キャラクター
- 複数ラボ
- 季節背景
- 長尺会話
- 高度な3Dや物理表現

## V0.9.3: 前景ドット絵の統一

少女5ポーズ・釜32コマ・小道具3点を再制作し、共通64色と前景ピクセル面で解像感を統一。サイズ比較と作業接点を更新。入力・World・保存の変更なし。

## V0.9.4: 道具間の歩行

既存の5つの移動経路に8コマの歩行と左右の向きを追加。距離で足運びを同期し、道具操作を到着後へ移す。一時停止と非表示からの再開を検証。自由な散策や経路探索は後続判断とする。

## V0.9.5: 手元の連続動作

状態: v0.9.5で実装・検証。2026-09-06のユーザー判断によるV1前の必須項目。

本の確認、素材取り出し、調合と完成品回収、トレー配置の4系列。44種類の全身原画を61の表示枠へ展開し、手と道具の接触、保持物の受け渡し、前後関係、歩行との連続性を検証した。[制作仕様とAC1〜9](docs/plans/v0.9.5-hand-motion.md)、[実装と検証](docs/hand-motion-verification.md)を参照。

## V0.9.6: GUI一時実行の保存境界

GUIの `--ephemeral` が既定保存先を再設定する不具合を修正。設定・発見を読まず保存しない契約をGUI/CLIで揃え、通常保存と明示診断ログを維持する。[検証記録](docs/ephemeral-verification.md)。V1の実機確認とは独立した修正。

## V0.9.7: 初回の活動取得選択と既存ユーザーへの説明

新規GUIは活動取得を選んでから開始する。既存ユーザーはON/OFFを維持して非モーダルの説明欄を表示し、説明を閉じても取得設定を変えない。旧ONを同意履歴に変換しない。[使い方と検証](docs/activity-notice-verification.md)。V1全体の実機品質、起動seed、破損JSONの扱いなどは引き続き別のOODAで判断する。

## V0.9.8: セッションごとのSeed生成

GUI・CLIの省略時seedを起動ごとに生成する。明示 `--seed` で再現し、0・負数・GUIからCLIへのフォールバックでも指定条件を維持する。[使い方と検証](docs/session-seed-verification.md)。破損JSON、データ削除、取得停止ライフサイクル、V1実機/性能は別候補として残る。

## V1.0: MVPリリース

v0.9.9では、閉じたウィンドウをトレイから再表示した際のWorld更新を復帰させた。一時停止・活動OFFを維持し、終了済みセッションは再開しない。[検証記録](docs/tray-reopen-verification.md)。ロック中の時間進行、8時間連続試験、FPS、125%/200%を含むV1全体の合否は別途判断する。

v0.9.2では、背景の本棚化、独立した道具、3案比較後のサイズ確定、白縁・接地・釜の輪郭安定化、素材到着から完成品配置までの作業連携を実装した。V1全体の合否判断は含めない。

V0.9.1でラボビジュアル品質を強化済み。MVP RC判定と実機互換性検証の残件は引き続き別途判断する。

目的: V0.9の小さなラボラトリー表現とリリース候補条件を満たした後、最初の安定MVPを公開する。

前提: v0.9.5の手元4系列とAC1〜9を満たす。4系列の合格だけでV1全体の合格とはしない。

期待状態:

- 安全既定の活動観測
- 通常作業を邪魔しないデスクトップ常駐
- セッションベースのラボ状態生成
- 発見情報の永続化
- 基本設定とプライバシー制御
- 既知制限の明文化
- 再現可能なビルドとリリース手順

## 後回し候補

以下は、後続OODAで採用しない限り初期MVPの外に置きます。

- 高度な粒子/破片物理
- 複雑な生物AI
- 大規模な希少現象カタログ
- 外部コンテンツ配信
- 有料コンテンツ
- クラウド同期
- 詳細リプレイ
- 複数ワールド同時実行
- 高度なマルチモニター演出
